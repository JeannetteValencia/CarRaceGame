// to check if it is well linked--> console.log ("hello world");
//Object Oriented Programming
//obstacles have information (position), functionalit (move) reuse them.

const myGame = new Game();
myGame.startGame();

